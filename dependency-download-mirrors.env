# User-owned overrides for dependencies whose install scripts bypass the
# language package registry. Ordinary npm dependencies use NPM_CONFIG_REGISTRY.
#
# Example:
# npm_config_vendor_binary_host_mirror=${DEPENDENCY_GITHUB_RAW_URL}/vendor/project/releases/download/
